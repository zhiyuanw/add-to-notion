{
  "name": "add-to-notion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 extension for saving Confluence pages to Notion.",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build-extension.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vitest": "^1.6.0"
  }
}
